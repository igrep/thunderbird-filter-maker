module Main where

import Lib


main :: IO ()
main = do
  -- (1) parse mbox to list mailing lists
  -- (2) choose which filters to make from (1)'s output file
  -- (3) Make filters for mail addresses edited at (2).
  return ()
